[tool.ruff]
lint.select = ["E", "F", "B", "ANN"]
lint.ignore = ["E501"]  # Ignore long lines and self/cls type hints

[lint.flake8-annotations]
mypy-init-return = true  # Require return type on `__init__`